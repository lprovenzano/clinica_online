import { ShiftstatusPipe } from './shiftstatus.pipe';

describe('ShiftstatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftstatusPipe();
    expect(pipe).toBeTruthy();
  });
});
