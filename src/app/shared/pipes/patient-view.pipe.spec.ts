import { PatientViewPipe } from './patient-view.pipe';

describe('PatientViewPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientViewPipe();
    expect(pipe).toBeTruthy();
  });
});
