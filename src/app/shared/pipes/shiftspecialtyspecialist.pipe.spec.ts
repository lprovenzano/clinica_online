import { ShiftspecialtyspecialistPipe } from './shiftspecialtyspecialist.pipe';

describe('ShiftspecialtyspecialistPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftspecialtyspecialistPipe();
    expect(pipe).toBeTruthy();
  });
});
