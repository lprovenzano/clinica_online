import { ShiftcolorPipe } from './shiftcolor.pipe';

describe('ShiftcolorPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftcolorPipe();
    expect(pipe).toBeTruthy();
  });
});
