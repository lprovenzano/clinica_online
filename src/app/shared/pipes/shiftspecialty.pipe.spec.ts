import { ShiftspecialtyPipe } from './shiftspecialty.pipe';

describe('ShiftspecialtyPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftspecialtyPipe();
    expect(pipe).toBeTruthy();
  });
});
